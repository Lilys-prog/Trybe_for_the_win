// o filter gera um NOVO array, contendo os atributos especificados. Desta forma, o novo array não necessariamente terá o mesmo tamanho que o array original, só os elementos que satisfizerem a condição determinada pela função

// map -> novo array do tamanho do original
// sort -> realiza a ordenação do próprio array fornecido
// filter -> array novo com os elementos que atendem as condições

// o filter sempre traz o próprio elemento que atende a condição, sem alterá-lo

// o filter pode receber uma condicional para filtrar, não precisa usar o if