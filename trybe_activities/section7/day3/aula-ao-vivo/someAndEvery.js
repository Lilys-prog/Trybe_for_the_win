// as hofs .some() e .every() retornam um valor boolean false ou true.

// eles procuram o elemento determinado e dizem se encontraram

// .some() só precisa encontrar um elemento igual ao pedido para retornar true

// .every() precisa que o array inteiro seja composto pelo mesmo elemento para retornar true