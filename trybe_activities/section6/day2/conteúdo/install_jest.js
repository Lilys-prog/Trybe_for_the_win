// para instalar o jest, usa-se o comando 
// npm install --save-dev jest

// o trecho "--save-dev" garante que o framework só estará na área de desenvolvimento, não será adicionado ao deploy.
// depois da instalação é importante ir ao package.json e colocar "jest" no parâmetro de testes - se não estiver

// NÃO ESQUECER que ao iniciar um projeto deve-se usar o comando
                    // npm init -y
// para criar o arquivo package.json